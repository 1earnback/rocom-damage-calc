/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/rocom-damage-calc',
  images: {
    unoptimized: true,
  },
  reactStrictMode: true,
};

export default nextConfig;
